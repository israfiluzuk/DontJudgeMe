using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ElonMusk : Human
{
    // Start is called before the first frame update
    void Start()
    {
        PlayAnim(AnimationType.Yelling);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
